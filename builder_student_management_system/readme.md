Выполненные дополнительные задания (5 из 3 требуемых):
1. Fluent builder + делегаты + scope
Fluent интерфейс (все методы возвращают this)

Делегаты конфигурации: addStudent(configFn), addCourse(configFn)

Паттерн scope: withScope() для групповой конфигурации

2. Высокоуровневые функции конфигурации
setMultiple() - установка нескольких полей

copyFrom() - копирование из другого билдера

setPersonalInfo() - доступ к 2 полям с общим правилом

3. Улучшенная система валидации
Агрегация всех ошибок (не только первой)

Добавление stack trace для определения места создания

Многоуровневая валидация (сеттеры + build)

4. База данных как выходная структура
Объект db как имитация БД

Ссылки через ID вместо прямых ссылок

Автоматическое сохранение при build()

5. Immutable модели с глубокой заморозкой
Настоящая неизменяемость через _deepFreeze()

Возврат копий массивов для безопасности


Глубокая заморозка в immutable моделях - решает реальную проблему в JS

Агрегация ошибок с сохранением стека - полезно для отладки

Паттерн scope в GroupBuilder - элегантное решение для групповых настроек

Генерация ID в конструкторе - решает проблему логической целостности

Разделение ответственности между классами